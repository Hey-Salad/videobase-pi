# WebSocket URL for video streaming
# Leave empty to automatically derive from the UI host, or explicitly set your backend host/port
VITE_WS_URL=

# API URL for HTTP requests
VITE_API_URL=

# OpenAI GPT-OSS backend (HeySalad GPT directory runs Flask on 5000 by default)
# Leave empty to default to http://localhost:5000
VITE_GPT_BACKEND_URL=
