# WebSocket URL for video streaming
# Leave empty to automatically derive from the UI host, or explicitly set your backend host/port (e.g., ws://raspbx:9200/ws)
VITE_WS_URL=

# API URL for HTTP requests (device info, etc.)
# Leave empty to derive from the UI host, or set a full backend origin (e.g., http://raspbx:9200)
VITE_API_URL=

# OpenAI GPT-OSS backend URL
# Typically `http://localhost:5000` when running /home/admin/heysalad-gpt-oss-kiosk-backend/heysalad_backend.py locally
VITE_GPT_BACKEND_URL=
